name: FPL Daily Snapshot

on:
  schedule:
    - cron: '0 7 * * *'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  snapshot:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Smoke test (offline, stubbed API)
        run: python scripts/smoke_test.py

      - name: Fetch FPL snapshot
        run: python scripts/snapshot.py

      - name: Commit snapshot
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add snapshot.json data/element_summary_cache.json \
                  data/deadline_state calibration.csv
          if git diff --cached --quiet; then
            echo "No changes to snapshot.json"
          else
            git commit -m "Update daily FPL snapshot"
            git push origin HEAD:${{ github.ref_name }}
          fi
